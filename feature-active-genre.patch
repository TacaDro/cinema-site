diff --git a/index.php b/index.php
--- a/index.php
+++ b/index.php
@@ -1,20 +1,49 @@
 <?php
 require __DIR__.'/db.php';
 require __DIR__.'/csrf.php';
 $config = require __DIR__.'/config.php';
 
 $base = rtrim(dirname($_SERVER['SCRIPT_NAME']), '/\');
 if ($base==='.'||$base==='/') $base='';
 
+// Polyfill для PHP < 8 (на всякий случай)
+if (!function_exists('str_starts_with')) {
+  function str_starts_with($haystack, $needle) {
+    return strpos($haystack, $needle) === 0;
+  }
+}
+
 function h($s){return htmlspecialchars((string)$s,ENT_QUOTES|ENT_SUBSTITUTE,'UTF-8');}
 function slug($s){$s=iconv('UTF-8','ASCII//TRANSLIT',$s);$s=preg_replace('~[^a-z0-9]+~i','-',$s);return trim(strtolower($s),'-')?:'item';}
-function url_movie($id,$title){global $base;return "$base/film/$id-".slug($title);} 
-function render($view,$data=[]){extract($data,EXTR_SKIP);include __DIR__.'/views/_layout.php';}
+function genre_slug($g){ return strtolower(str_replace(' ','-',trim($g))); }
+function url_movie($id,$title){global $base;return "$base/film/$id-".slug($title);} 
+
+/**
+ * Список жанров для меню (DISTINCT по movies.genre)
+ */
+function get_genres(PDO $pdo, int $limit = 30): array {
+  $stmt = $pdo->query("SELECT DISTINCT genre FROM movies WHERE genre<>'' ORDER BY genre ASC");
+  $list = [];
+  foreach ($stmt as $row) {
+    $name = trim($row['genre']);
+    if ($name==='') continue;
+    $list[] = ['name'=>$name, 'slug'=>genre_slug($name)];
+  }
+  return array_slice($list, 0, $limit);
+}
+
+/**
+ * Рендер с автопробросом меню жанров во все страницы
+ * и поддержкой $active_genre_slug для подсветки активного.
+ */
+function render($view,$data=[]){
+  extract($data,EXTR_SKIP);
+  global $pdo;
+  $genres = get_genres($pdo);
+  include __DIR__.'/views/_layout.php';
+}
 function require_admin(){if(empty($_SESSION['is_admin'])){global $base;header('Location: '.($base?:'').'/admin/login');exit;}}
 
 $uri=parse_url($_SERVER['REQUEST_URI'],PHP_URL_PATH);
 $path=$base&&str_starts_with($uri,$base)?substr($uri,strlen($base)):$uri;
 $path='/'.ltrim($path,'/');
 
 // Главная
 if($path==='/'||$path==='/index.php'){
   $q=trim($_GET['q']??'');
   $sql='SELECT id,title,`year`,genre,poster_url FROM movies';
   $p=[]; if($q!==''){ $sql.=' WHERE title LIKE :q'; $p[':q']="%$q%"; }
   $sql.=' ORDER BY id DESC LIMIT 200';
   $st=$pdo->prepare($sql);$st->execute($p);$movies=$st->fetchAll();
-  return render('home',compact('movies','q','base'));
+  $active_genre_slug = null;
+  return render('home',compact('movies','q','base','active_genre_slug'));
 }
 
 // Фильм
 if(preg_match('~^/film/(\d+)~',$path,$m)){
   $id=(int)$m[1];$st=$pdo->prepare('SELECT * FROM movies WHERE id=:id');$st->execute([':id'=>$id]);$movie=$st->fetch();
   if(!$movie){http_response_code(404);return render('404',compact('base'));}
-  return render('movie',compact('movie','base'));
+  $active_genre_slug = null;
+  return render('movie',compact('movie','base','active_genre_slug'));
 }
 
 // Жанр
 if(preg_match('~^/genre/([a-z0-9-]+)$~',$path,$m)){
   $slug=$m[1];
   $st=$pdo->prepare('SELECT id,title,year,genre,poster_url FROM movies WHERE LOWER(REPLACE(genre," ","-")) LIKE :g ORDER BY id DESC');
   $st->execute([':g'=>strtolower($slug).'%']);
   $movies=$st->fetchAll();
   $genre_name=str_replace('-',' ',$slug);
-  return render('genre',compact('movies','genre_name','base'));
+  $active_genre_slug = $slug;
+  return render('genre',compact('movies','genre_name','base','active_genre_slug'));
 }
 
 if($path==='/poisk'){$q=trim($_GET['q']??'');header('Location: '.($base?:'').'/?q='.urlencode($q));exit;}
 
diff --git a/views/_layout.php b/views/_layout.php
--- a/views/_layout.php
+++ b/views/_layout.php
@@ -1,14 +1,33 @@
 <?php /* Базовый шаблон */ ?>
 <!doctype html>
 <html lang="ru">
 <head>
   <meta charset="utf-8" />
   <meta name="viewport" content="width=device-width, initial-scale=1" />
   <title><?= isset($movie) ? h($movie['title']).' — Фильмы' : 'Фильмы' ?></title>
-  <link rel="stylesheet" href="<?= $base ?>/style.css" />
+  <link rel="stylesheet" href="<?= $base ?>/style.css?v=3" />
 </head>
 <body>
 <header class="container">
   <h1><a href="<?= $base ?>/">Фильмы</a></h1>
   <form class="search" action="<?= $base ?>/poisk" method="get">
     <input type="text" name="q" placeholder="Название" value="<?= h($q ?? '') ?>" />
     <button>Найти</button>
   </form>
 </header>
 
+<?php
+  // Меню жанров: ожидает $genres (массив) и $active_genre_slug (строка или null)
+  $active = $active_genre_slug ?? null;
+?>
+<?php if (!empty($genres)): ?>
+  <nav class="genres-nav container">
+    <ul>
+      <?php foreach ($genres as $g): 
+        $isActive = ($active && $g['slug'] === $active);
+      ?>
+        <li>
+          <a href="<?= $base ?>/genre/<?= h($g['slug']) ?>" class="<?= $isActive ? 'active' : '' ?>">
+            <?= h($g['name']) ?>
+          </a>
+        </li>
+      <?php endforeach; ?>
+    </ul>
+  </nav>
+<?php endif; ?>
+
 <main class="container">
   <?php include __DIR__ . '/' . $view . '.php'; ?>
diff --git a/style.css b/style.css
--- a/style.css
+++ b/style.css
@@ -1,20 +1,42 @@
 *{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:#0b0f14;color:#e6edf3}
 a{color:#8ab4ff;text-decoration:none}
 .container{max-width:1100px;margin:0 auto;padding:16px}
 header{display:flex;gap:16px;align-items:center;border-bottom:1px solid #223}
 header h1{margin:0;font-size:20px}
 .search{margin-left:auto;display:flex;gap:8px}
 input[type=text]{padding:10px;border:1px solid #334;border-radius:8px;background:#11161d;color:#e6edf3}
 button{padding:10px 14px;border:0;border-radius:8px;background:#1f6feb;color:#fff;cursor:pointer}
 .muted{color:#9aa7b8}
 .grid{list-style:none;display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:16px;padding:16px 0;margin:0}
 .card{background:#0e141b;border:1px solid #223;border-radius:12px;overflow:hidden}
 .card img{width:100%;height:320px;object-fit:cover;display:block}
 .card .meta{padding:12px}
 .movie{display:grid;grid-template-columns:280px 1fr;gap:20px;padding:20px;background:#0e141b;border:1px solid #223;border-radius:12px}
 .poster img{width:100%;height:auto;border-radius:8px}
 .table{width:100%;border-collapse:collapse;margin:12px 0}
 .table th,.table td{border:1px solid #223;padding:8px}
 .btn{display:inline-block;padding:8px 12px;border-radius:8px;background:#1f6feb;color:#fff;margin-left:6px}
 .btn.danger{background:#b23}
 label{display:block;margin:10px 0}
 input[type=number],input[type=url],textarea{width:100%;padding:10px;border:1px solid #334;border-radius:8px;background:#11161d;color:#e6edf3}
 
+/* Меню жанров + активный элемент */
+.genres-nav{border-bottom:1px solid #223}
+.genres-nav ul{list-style:none;margin:0;padding:8px 0;display:flex;flex-wrap:wrap;gap:8px}
+.genres-nav li a{display:inline-block;padding:6px 10px;border:1px solid #223;border-radius:999px;background:#0e141b}
+.genres-nav li a:hover{filter:brightness(1.1)}
+.genres-nav li a.active{
+  background:#1f6feb;
+  color:#fff;
+  border-color:#1f6feb;
+}
